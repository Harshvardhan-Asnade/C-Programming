#include <stdio.h>
int main()
{
    int n;
    printf("Enter a nUmber");
    scanf("%d", &n);
    if (n>99==0 && n>1000==0)
    {
        printf("IT IS A THREE DIGIT NUMBER");
    }
    else{
        printf("IT IS NOT A THREE DIGIT NUMBER");
    }
    return 0;
} 